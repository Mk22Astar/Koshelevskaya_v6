<Window x:Class="Koshelevskaya_v6.AddServiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить услугу" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Название услуги:" Grid.Row="0" />
        <TextBox x:Name="serviceNameTextBox" Grid.Row="1" />

        <TextBlock Text="Описание услуги:" Grid.Row="2" />
        <TextBox x:Name="descriptionTextBox" Grid.Row="3" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4" Margin="0,10,0,0">
            <Button Content="Отмена" Click="Cancel_Click" Width="100" Margin="0,0,10,0" />
            <Button Content="Сохранить" Click="Save_Click" Width="100" />
        </StackPanel>
    </Grid>
</Window>